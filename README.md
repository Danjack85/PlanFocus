# 计划事件 · PlanFocus（发布页）

本仓库**仅用于「计划事件」应用的内置自动更新**，不放源码 —— 只有 Release 里会附上安装包。

## 应用简介

把"行程计划 → 到点提醒 → 专注计时"串成闭环的 Android 应用：录入一天的计划，
到点响铃/语音/震动提醒，关铃即开始专注计时并记录分心情况。

- 计划管理、重复规则（只响一次 / 每天 / 按星期）、文本批量导入
- **想法箱**：随手记下想到的事，之后一键安排成计划
- **今日日程**：一屏看完今天安排，直接打勾、汇总"今天要准备"的清单
- **准备清单**：每条计划可列出要带/要先做的事，到点提醒时直接显示
- 专注时长可按"多少分钟"或"**专注到某时刻**"来设（如 12:15 提醒、专注到 16:45），跨午夜自然支持
- 计时中可随时调整目标（改到某个时刻，或"再专注 25 分钟"）
- 精确到点提醒（一主一备双闹钟 + 定期自愈 + 错过补提醒）
- 提醒方式可选：铃声、语音、震动、**仅震动静音模式**
- **随时开始专注**：通知栏按钮 / 下拉快捷面板磁贴 / 长按桌面图标的快捷方式（含最近 3 条计划）
- **桌面小组件**：显示下一项计划与倒计时，可一键开始专注
- **结束有反馈**：达成时长与中途结束音效不同，且**不会打断你正在听的音乐**
- 专注计时：暂停/继续、长按结束、可选"退出页面后继续计时"
- **专注统计**：今天 / 近 7 天 / 近 30 天 / 选某天；柱状图、环形占比、趋势曲线、当天时间轴、
  打卡热力图、**生产力时段**、计划排行与分心 App 排行、小结洞察
- **目标与成就**：每日 / **每周 / 每月目标进度**、连续打卡天数、6 项成就徽章
- **数据自主**：CSV 导出（无需存储权限）、手动补录专注记录
- 界面：开屏动画、底部导航、深色模式与纯黑背景、动态取色（Material You）、自适应图标（含 Android 13 单色主题图标）
- 应用使用时长监控、每日上限报警、分心 App 明细
- 通知栏常驻倒计时（大字显示距下一项还有多久）
- **手环快应用**：小米 Vela 手表/手环上显示下一项计划与倒计时、到点震动、可在手环上添加计划

## 下载安装

到 [Releases](../../releases) 页下载最新的 `PlanFocus-*.apk`，在手机上安装即可
（系统会提示"未知来源"，允许一次）。需要 **Android 8.0 及以上**。

应用内也可以在「设置 → 版本更新」里检查更新，会直接下载并校验签名后引导安装。

手环端（可选）下载同一个 Release 里的 `PlanFocus-watch-release.rpk`，
用 AstroBox-NG 之类的工具装到手表上；**仅支持小米 Vela 系统手环/手表**，
且需要手机装「小米运动健康」并已完成配对。

## 最近更新

### v1.19

- **改**：设置页从"12 个分区全平铺"重构为 **7 个分类入口 + 子页**，
  入口卡上直接显示当前状态（如"仅震动"、"已常驻（显示到秒）"、"有 2 项权限待开启"）
- **简化**：权限缺失时置顶提示；子页只显示当前该处理的事；语音口令输入框收进开关；
  更新仓库默认收起；长篇说明文案收紧
- **交互**：进子页后按返回键先回分类列表，再按一次才退出设置

### v1.18

- **新**：想法箱 —— 先把想到的事记下来，之后一键安排成带提醒的计划（含时间提示、安排后自动移除）
- **新**：今日日程页 —— 一屏看完今天所有安排，直接打勾"今天做完了"，并汇总"今天要准备"的清单条目
- **新**：每条计划可写「准备清单」，到点提醒时直接显示在提醒页上（"记得准备好：带笔记本"）
- **修**：今日日程的星期名在英文系统上会显示成 Thursday（应用为全中文界面）

### v1.17

- **新**：不用打开应用就能开始专注 —— 通知栏按钮、下拉快捷面板磁贴、长按桌面图标（含最近计划的动态快捷方式）
- **新**：桌面小组件「下一项计划 + 一键开始专注」
- **新**：专注结束的音效与震动（达成与中途结束不同；用音频焦点压低音乐而不打断）
- **新**：统计页加周/月目标进度（含"每天还需多久"）、生产力时段分析、6 项成就徽章
- **新**：CSV 导出（带 BOM，Excel 中文不乱码）、手动补录专注

### v1.16

- **新**：计划可设"专注到某时刻"（12:15 提醒 → 专注到 16:45），跨午夜按第二天算
- **新**：计时中可随时调整目标
- **修**：选"到某时刻"时目标漏算已专注部分，会提前宣布达成
- **修**："目标 xx:xx 结束"在暂停过后会越来越晚

### v1.15

- **修**：统计页大数字折行、"分"与下面文字叠在一起
- **修**："选某天"点了没反应（日期块漏了点击事件）
- **修**："比上一周期 +59016%" 这类离谱百分比
- **新增**：当天 24 小时时间轴、计划排行横向条、热力图色阶图例、小结洞察、明细按天分组

### v1.14

- **修**：计时误差（22:00 开始的计划在 22:18 显示成 25:48 —— 进程重启后重复累加）
- **修**：手环连接慢/连不上、手环"全部计划"空白
- **升级**：手机↔手环握手协议 v2（版本协商、心跳、请求编号去重、离线缓存）
- **革新**：开屏动画、新图标、底部导航、今日概览卡、深色模式、每日目标

完整说明见 [Releases](../../releases) 里各版本的发布说明。

## 说明

- 安装包只在本仓库的 Release 附件里提供，不做任何形式的静默安装。
- 本应用为自用工具，不使用无障碍、设备管理员等敏感权限。
